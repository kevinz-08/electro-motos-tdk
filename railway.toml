[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @h2r/database generate && pnpm --filter @h2r/api build"

[deploy]
startCommand = "node apps/api/dist/main.js"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
